# 금융 투자 대시보드 기획서

**프로젝트명:** DAKER — 금융 투자 분석 대시보드  
**제출일:** 2026-04-10  
**개발자:** BaeYoungWhan  
**플랫폼:** Streamlit Cloud  
**서비스 URL:** https://dakerskills.streamlit.app  
**GitHub:** https://github.com/BaeYoungHwan/DAKER_SKILL

---

## 1. 프로젝트 개요

### 1.1 한 줄 소개

> **Skills.md로 규칙을 정의하고, 바이브 코딩으로 구현한 실시간 금융 투자 분석 대시보드**

### 1.2 목적

개인 투자자가 주식·ETF 데이터를 직관적으로 분석하고, 투자 의사결정을 지원하는 도구입니다.  
별도의 API 키나 유료 서비스 없이 **Yahoo Finance(yfinance)** 를 통해 글로벌 및 한국 주식 실시간 데이터를 조회합니다.

### 1.3 핵심 차별점

| 항목 | 내용 |
|------|------|
| API Key 불필요 | yfinance 기반, 별도 인증 없이 즉시 실행 |
| 한국 주식 지원 | 한국어 종목명(삼성전자, 카카오 등) 검색 → 자동 티커 변환 |
| 혼합 통화 처리 | KRW/USD 혼합 포트폴리오 실시간 환율 자동 적용 |
| Skills 기반 자동화 | 분석·시각화·인사이트 규칙을 문서로 정의 → AI가 코드로 구현 |
| 자동 리포트 생성 | 분석 결과를 .txt 리포트로 다운로드 |

---

## 2. 바이브코딩 방법론

### 2.1 Skills.md → AI → Code 파이프라인

이 프로젝트는 **"규칙을 먼저 문서화하고, AI가 코드를 생성"** 하는 바이브코딩 방식으로 개발되었습니다.

```
Skills/analysis.md      →  투자 지표 계산 규칙 정의
Skills/visualization.md →  차트 선택 기준 및 색상 시스템 정의
Skills/insight.md       →  인사이트 등급·형식·우선순위 정의
        │
        ▼  (Claude Code — 바이브 코딩)
        │
src/data/fetcher.py     →  yfinance 데이터 수집 모듈
src/analysis/indicators.py  →  기술적 지표 계산 모듈
src/viz/charts.py       →  Plotly 차트 컴포넌트 모듈
src/app.py              →  Streamlit UI 통합 앱
```

### 2.2 Skills 런타임 파싱

Skills 문서에 정의된 수치(RSI 기준 14일, 과매수 70, 샤프비율 양호 기준 1.0 등)는  
**`src/skills/parser.py`** 가 런타임에 마크다운을 파싱하여 코드에 주입합니다.

```
Skills/analysis.md 수정
        │ (앱 재시작 즉시 반영)
        ▼
skills/parser.py → load_analysis_config() → lru_cache
        │
        ▼
indicators.py · app.py → 동적 임계치 적용
```

→ **코드 수정 없이 분석 규칙 변경 가능** — Skills.md가 곧 설정 파일

### 2.3 Skills 문서 구성

#### `Skills/analysis.md` — 분석 규칙
- 지원 데이터 유형 정의 (주가, ETF, 재무지표, 시장지수, 섹터)
- 수익률 계산 공식 명세
- RSI, 이동평균, 볼린저밴드, 변동성, 샤프비율 임계치 정의
- 포트폴리오 분산도 평가 기준 (단일 종목 >30%, 섹터 >40% 경고)

#### `Skills/visualization.md` — 시각화 규칙
- 데이터 유형별 차트 선택 기준 (캔들스틱/라인/히트맵/산점도)
- TradingView 다크 테마 색상 토큰 시스템 (17개 색상 토큰 정의)
- 차트 색상 상수 정의 (MA5/20/60/120/200별 색상)
- 레이아웃 그리드 규칙 (2/3 : 1/3 분할 등)

#### `Skills/insight.md` — 인사이트 생성 규칙
- 3단계 형식: 신호 → 해석 → 행동 제안
- 등급별 임계치: High/Medium/Low
- 인사이트 우선순위 규칙 (High 최상단, 종목당 최대 3개)
- 리포트 5섹션 구성 흐름 정의

#### `Skills/interaction.md` — UI 상태 관리 규칙
- Session State 초기화 키 정의 (`selected_tickers`, `ticker_names`, `holdings` 등)
- 워치리스트 영속성: `watchlist.json` UTF-8 저장·로드 흐름
- 검색 카운터 패턴: 종목 추가 후 검색창 자동 초기화
- URL 파라미터 처리: `?tickers=005930.KS` 앱 로드 시 자동 추가

---

## 3. 핵심 기능 목록

### 3.1 사이드바 — 컨트롤 패널

| 기능 | 상세 |
|------|------|
| 종목 검색 | 한국어 이름 또는 글로벌 티커로 검색, 드롭다운 선택 |
| 직접 티커 입력 | 쉼표 구분 입력 (Expander 내부) |
| 워치리스트 영속화 | `watchlist.json` 저장, 새로고침 후에도 종목 유지 |
| 분석 기간 | 1M / 3M / 6M / 1Y / 2Y / 5Y |
| 차트 옵션 | MA(5/20/60/120/200) 멀티셀렉트, 볼린저밴드, MACD 토글 |
| 벤치마크 | S&P500 / KOSPI / NASDAQ / DOW JONES / 없음 |
| 포트폴리오 비중 | 종목별 % 직접 입력 (합계 100% 유효성 검증) |

### 3.2 탭 1 — 종목 분석

| 컴포넌트 | 상세 |
|----------|------|
| 시장 지수 스트립 | S&P500, NASDAQ, DOW, KOSPI, VIX 실시간 카드 |
| 공포/탐욕 지수 | VIX 기반 Fear & Greed 게이지 바 |
| 헤더 배너 | 회사명·티커·섹터·현재가·전일대비·라이브 표시등 |
| KPI 카드 (5개) | 현재가 / 누적수익률 / 연환산변동성 / 샤프비율 / MDD |
| 재무지표 카드 (4개) | PER / PBR / EPS / 배당수익률 |
| 모멘텀 배지 | 1주·1개월·3개월 수익률 (상승↑/하락↓ 색상) |
| 52주 범위 바 | 저가~고가 트랙 위 현재 위치 표시 |
| 캔들스틱 차트 | OHLCV + MA 오버레이 + 볼린저밴드 + 거래량 |
| MACD 차트 | MACD·시그널·히스토그램 |
| RSI 섹션 | 반원형 게이지 + 라인차트 (과매수/과매도선) |
| 스토캐스틱 | %K·%D 라인 + 과매수80/과매도20 기준선 |
| 드로다운 차트 | 최대낙폭 시각화 (MDD 수치 → 곡선 차트) |
| 분기별 재무 | 매출·순이익·매출총이익 분기별 바 차트 (EPS 어닝서프라이즈) |
| 배당 히스토리 | 연도별 배당금 + 배당수익률 추이 |
| 실적 캘린더 | 다음 실적 발표일 D-day + EPS 서프라이즈(예상 vs 실제) + 발표 14일 이내 변동성 경고 |
| 기관 수급 현황 | 주요 보유 비중(외국인·기관·내부자%) + 상위 8개 기관 보유 테이블 |
| 최신 뉴스 | yfinance 기반 최신 뉴스 링크·출처·날짜 |

### 3.3 탭 2 — 포트폴리오

| 컴포넌트 | 상세 |
|----------|------|
| KPI 스트립 | 포트폴리오 수익률 / 최고 종목 / 최저 종목 / 평균 샤프 |
| 혼합 통화 처리 | KRW+USD 혼합 포트폴리오 실시간 환율 자동 적용 |
| 매입가/수량 입력 | 평가손익·손익률 자동 계산 |
| 종목별 성과 테이블 | 현재가·수익률·변동성·샤프·MDD·비중·베타 |
| 포트폴리오 수익률 차트 | 가중 누적수익률 (vs 벤치마크 비교) |
| 롤링 샤프 비율 | 시간에 따른 샤프비율 추이 |
| 파이차트 | 종목별 비중 도넛차트 |
| 리스크 경고 | 자산집중·섹터집중·고상관 자동 감지 및 경고 |

### 3.4 탭 3 — 비교 분석

| 컴포넌트 | 상세 |
|----------|------|
| 수익률 비교 차트 | 전 종목 누적수익률 멀티라인 (기준점=0%) |
| 기간별 수익률 테이블 | 1M·3M·6M·1Y 색상 코딩 비교 |
| 상관관계 히트맵 | 종목 간 상관계수 매트릭스 |
| 리스크-수익률 산점도 | 변동성(X) vs 수익률(Y) 버블 차트 |
| 섹터 히트맵 | 11개 섹터 ETF 기간별 성과 비교 |

### 3.5 탭 4 — 투자 인사이트

| 컴포넌트 | 상세 |
|----------|------|
| 전략 백테스트 | MA 크로스(MA20/MA60) · RSI 반전(30/70) 전략 수익률 시뮬레이션, 초기 자본 설정, 거래 내역 확인 |
| 매크로 지표 | USD/KRW 환율, 미국 10년물 국채, 달러인덱스 추이 멀티라인 차트 |
| 종목 스크리너 | RSI<30/70, 골든크로스·데드크로스, BB 돌파, MACD 전환, 52주 신고가, 거래량 N배↑, 저PER+저RSI 복합 조건 신호 테이블 |
| 인사이트 카드 | High·Medium·Info 등급별 색상 카드 (신호→해석→행동 3단계) |
| 종합 분석 요약 | 수익률·변동성·샤프비율 기반 종합 의견 |
| 투자 리포트 다운로드 | 핵심지표·기술신호·행동제안 포함 .txt 파일 |

### 3.6 외부 시스템 연동

URL 쿼리 파라미터를 통해 외부 시스템에서 DAKER로 종목을 자동 전달할 수 있습니다.

| 항목 | 내용 |
|------|------|
| 파라미터 | `?tickers=티커코드` (Yahoo Finance 형식, 예: `005930.KS`) |
| 복수 종목 | 쉼표 구분 (`?tickers=005930.KS,000660.KS`) |
| 동작 | 앱 로드 시 워치리스트에 자동 추가 (중복 제거) |
| 활용 예 | 자동매매 대시보드 포지션 탭의 📊 버튼 → DAKER 바로 연동 |

```
https://dakerskills.streamlit.app?tickers=005930.KS
```

---

## 4. 기술 아키텍처

### 4.1 기술 스택

| 구분 | 기술 |
|------|------|
| 언어 | Python 3.12 |
| UI 프레임워크 | Streamlit |
| 데이터 수집 | yfinance (Yahoo Finance API Wrapper) |
| 시각화 | Plotly (캔들스틱, 히트맵, 게이지, 산점도 등) |
| 데이터 처리 | pandas, numpy |
| 배포 | Streamlit Cloud |

### 4.2 모듈 구조

```
src/
├── app.py                  # Streamlit 메인 앱 (UI 통합, 탭 구성)
├── skills/
│   └── parser.py           # Skills.md 런타임 파서
│                           #   - load_analysis_config(): RSI·MA·샤프 임계치 로드
│                           #   - load_visualization_config(): 색상 토큰 로드
│                           #   - load_insight_rules(): 인사이트 규칙 테이블 파싱
│                           #   - lru_cache로 파일 1회만 파싱 (성능 최적화)
├── data/
│   ├── base.py             # DataProvider 추상 인터페이스 (ABC)
│   ├── yfinance_provider.py# YFinanceProvider 구현체
│   └── fetcher.py          # yfinance 래퍼 (캐싱 포함)
│                           #   - fetch_price(): OHLCV 조회 + Rate Limit 재시도
│                           #   - fetch_multiple(): 멀티 종목 병렬 조회 (ThreadPoolExecutor)
│                           #   - fetch_info(): 재무지표·메타데이터
│                           #   - fetch_financials(): 분기별 실적
│                           #   - fetch_dividends(): 배당 히스토리
│                           #   - fetch_news(): 최신 뉴스
│                           #   - fetch_exchange_rate_series(): USD/KRW 환율
├── analysis/
│   └── indicators.py       # 기술적 지표 계산 (임계치는 parser.py에서 로드)
│                           #   - RSI, MACD, 볼린저밴드, 스토캐스틱
│                           #   - 드로다운, 샤프비율, MDD, 변동성
│                           #   - 골든/데드크로스 탐지, 모멘텀
└── viz/
    └── charts.py           # Plotly 차트 컴포넌트 (색상은 parser.py에서 로드)
                            #   - candlestick_chart(), rsi_chart(), rsi_gauge_chart()
                            #   - macd_chart(), stochastic_chart()
                            #   - drawdown_chart(), correlation_heatmap()
                            #   - sector_heatmap(), risk_return_scatter()
                            #   - rolling_sharpe_chart(), earnings_chart()
```

### 4.3 데이터 흐름

```
사용자 입력 (티커·기간·옵션)
        │
        ▼
fetcher.py → yfinance API → 원시 데이터 (OHLCV, 재무, 뉴스)
        │
        ▼
indicators.py → 기술적 지표 계산 (RSI, MACD, 볼린저밴드 등)
        │
        ▼
charts.py → Plotly Figure 생성
        │
        ▼
app.py → Streamlit 렌더링 → 사용자 화면
```

### 4.4 색상 시스템 (TradingView Dark 테마)

| 토큰 | 값 | 용도 |
|------|----|------|
| `--bg` | `#131722` | 앱 전체 배경 |
| `--card` | `#1E222D` | 카드·패널 배경 |
| `--accent` | `#2962FF` | 강조색 |
| `--up` | `#26a69a` | 상승·긍정 |
| `--down` | `#ef5350` | 하락·부정 |

---

## 5. 한국 주식 지원

### 5.1 하이브리드 검색 방식

1. **Skills/symbols.json** — 70개+ 주요 종목 외부 파일 매핑 (삼성전자 → 005930.KS), `src/skills/parser.py`의 `load_kr_symbols()`가 런타임에 로드
2. **yfinance 검색 API** — symbols.json 미등록 종목 실시간 검색
3. **중복 제거** — 공식 회사명 우선 (긴 이름 → 더 정확한 결과)

### 5.2 지원 카테고리 (70개+)

- 반도체·전자: 삼성전자, SK하이닉스, LG전자 등
- 2차전지: LG에너지솔루션, 삼성SDI, 에코프로비엠 등
- 바이오: 삼성바이오로직스, 셀트리온, 유한양행 등
- 금융·보험: KB금융, 신한지주, 삼성생명 등
- 통신·플랫폼: 카카오, 네이버, SK텔레콤 등
- 자동차·철강·건설: 현대차, 기아, POSCO 등

---

## 6. 안정성 및 데이터 품질

### 6.1 Rate Limit 핸들링
- `fetch_price()`: 실패 시 최대 3회 자동 재시도 (지수 백오프)
- `fetch_multiple()`: 종목 간 0.3초 딜레이로 API 과부하 방지

### 6.2 데이터 이상치 처리
- 일별 수익률 ±15% 초과 시 이상치 플래그 (기준은 Skills/analysis.md에서 런타임 로드)
- 이상치 경고: 종목당 최대 3건 + 접힘(collapsed) 표시로 UX 방해 최소화
- 결측값: forward fill 처리 (Skills/analysis.md §4 기준)

### 6.3 오류 방어
- 존재하지 않는 티커 입력 시 앱 크래시 없이 graceful 처리
- 데이터 없는 재무항목은 UI에서 자동 미표시
- 단일 종목 / 멀티 종목 모두 오류 없이 동작

---

## 7. 실행 방법

### 로컬 실행

```bash
# 의존성 설치
pip install -r requirements.txt

# 앱 실행
streamlit run src/app.py
```

브라우저 `http://localhost:8501` 접속

### Streamlit Cloud 배포

GitHub 연동 후 자동 배포  
`requirements.txt` 기반 패키지 자동 설치

---

## 8. 향후 개선 계획

| 우선순위 | 항목 | 내용 |
|----------|------|------|
| Medium | 알림 기능 | RSI 임계치 도달 시 브라우저 푸시 알림 |
| Low | PDF 리포트 | 분석 결과 PDF 다운로드 (현재 .txt 제공) |
| Low | 다크/라이트 모드 토글 | 사용자 선택 테마 |

---

## 9. 평가 기준 대응

| 평가 항목 | 대응 내용 |
|-----------|-----------|
| 범용성 | fetcher / indicators / charts / app 모듈 분리, DataProvider 추상화로 데이터 소스 교체 가능 |
| Skills.md 설계 | 분석·시각화·인사이트 3개 문서, 임계치·색상코드·형식 명확히 정의. 런타임 파싱으로 코드 수정 없이 규칙 변경 |
| 대시보드 자동 생성 | Skills 규칙 기반 KPI·차트·인사이트 자동 구성, 탭 4개 완성. 리포트 .txt 다운로드 |
| 바이브코딩 활용 | Skills.md → Claude Code → src/ 전 흐름을 AI가 코드 생성. 임계치까지 Skills 파일로 관리 |
| 실용성·창의성 | API Key 불필요, 한국어 검색(70개+), KRW/USD 혼합통화 실시간 환율, 자동 리포트, URL 파라미터 외부 연동 |

---

*작성일: 2026-04-10*
